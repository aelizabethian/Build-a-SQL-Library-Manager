extends layout 

block content 
    head
        h1.newBook New Book
        include form-error
        .container
            form(method="post")
                
                p.formInput
                    label(for="title") Title
                    input#title(name="title" type="text" value= book.title)
                p.formInput
                    label(for="author") Author
                    input#author(name="author" type="text" value= book.author)
                p.formInput
                    label(for="genre") Genre
                    input#genre(name="genre" type='text' value= book.genre)
                p.formInput
                    label(for='year') Year
                    input#year(name='year' type='text' value= book.year)
                p
                    input#createBtn(type='submit' value='Create New Book')
                p
                    button.cancelBtn
                        a(href='/books') Cancel